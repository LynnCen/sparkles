import { FC } from 'react';
import styles from './entry.module.less';
const Expandsotre: FC<any> = () => {
  return (
    <div className={styles.container}>
    </div>
  );
};

export default Expandsotre;
