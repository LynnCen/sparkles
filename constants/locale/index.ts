import zh from './zh'
import en from './en'
import tr from './tr'

export const supportLanguage = [
    "en",
    "tr",
    "zh",
]

export const locales = {
    zh,
    en,
    tr
}

export default locales
